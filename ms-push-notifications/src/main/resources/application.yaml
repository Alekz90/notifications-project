server:
  port: 8083

spring:
  application:
    name: ms-push-notifications

  redis:
    host: localhost
    port: 6379
    password: ""
    jedis:
      pool:
        max-active: 10
        max-idle: 5
        min-idle: 1
        max-wait: -1ms
